<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.qiyi.video.readerdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".motion.MotionActivity" />
        <activity android:name=".motion.DemoActivity"/>
        <activity android:name=".motion.ViewPagerActivity"/>
        <activity android:name=".motion.ViewPagerActivity2"/>
        <activity android:name=".motion.FragmentExampleActivity"/>
        <activity android:name=".motion.FragmentExample2Activity"/>
        <activity android:name=".motion.YouTubeDemoActivity"/>
    </application>

</manifest>